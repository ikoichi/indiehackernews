import { Flex, FlexProps } from "@chakra-ui/react";
import React from "react";

export const Brand = (props: FlexProps) => {
  return (
    <Flex {...props}>
      👩‍💻🧑‍💻
    </Flex>
  );
};
