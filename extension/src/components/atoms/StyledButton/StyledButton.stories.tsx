import React from "react";
import { StyledButton } from "./StyledButton";

export default {
  title: "StyledButton",
  component: StyledButton,
};

export const Base = () => <StyledButton>Sign up</StyledButton>;
